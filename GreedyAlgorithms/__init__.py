"""
A greedy algorithm:
    - computes a solution in steps, where at each step the algorithm makes a decision that is locally
      optimum, and it never changes that decision.
    - does not necessarily produce the optimum solution.

"""